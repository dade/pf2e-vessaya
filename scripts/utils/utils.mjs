export const loc = (path) => game.i18n.localize(path)
export const locPath = (path) => game.i18n.localize(`pf2e-vessaya.settings.category.${path}`)
export const locSettings = (path) => game.i18n.localize(`pf2e-vessaya.settings.${path}`)
export const fmt = (key, data) => game.i18n.format("pf2e-vessaya.ammoSysmte.check." + key, data)


