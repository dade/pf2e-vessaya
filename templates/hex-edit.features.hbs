<div class="form-group feature">
	<select name="feature.{{id}}.type">
		{{selectOptions features selected=type labelAttr="label" black="" localize=true}}
	</select>
	<input type="text" name="features.{{id}}.name" value="{{name}}" />
	<input
		type="checked"
		name="features.{{id}}.discovered"
		{{checked discovered}}
		data-tooltip="VESSAYA.HEXTOOL.Discovered"
	/>
	<button 
		data-tooltip="Remove"
		type="button"
		class="icon fa-solid fa-times"
		data-action="removeFeature"
	></button>
</div>
